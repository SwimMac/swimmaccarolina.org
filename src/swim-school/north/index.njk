---
layout : base.njk
title : Swim School North
---
<main id="main" class="bg-gray-100 overflow-hidden">
    {% banner title, {image: 'hffa.jpg'} %}

    <div class="container my-6">
        <p>
            <a href="/">Home</a> /
            <a href="/swim-school/">Swim School</a> /
            {{ title }}
        </p>

        {% include 'covid-notice.njk' %}

       <div class="flex flex-wrap -mx-4">

           {% for group in swim_school.groups %}
            <div class="w-full md:w-1/2 lg:w-1/3 p-4">
                <a href="/swim-school/north/{{ group.url }}" class="card h-full overflow-hidden">
                    {% if group.image %}
                    <div class="card-image h-64 bg-center bg-cover" style="background-image: url('/static/cards/{{ group.image }}');"></div>
                    {% endif %}
                    <div class="p-4">
                        <h2>{{ group.name }}</h2>
                        <p><strong>{{ group.ages }}</strong></p>
                        <p>{{ group.description }}</p>
                    </div>
                </a>
            </div>
            {% endfor %}

            <div class="w-full md:w-1/2 lg:w-1/3 p-4">
                <a href="/private-lessons/private-lessons-north/" class="card h-full overflow-hidden">
                    <div class="card-image h-64 bg-center bg-cover" style="background-image: url('/static/cards/private-lessons.jpg');"></div>
                    <div class="p-4">
                        <h2>Private Lessons</h2>
                        <p><strong>All Ages</strong></p>
                        <p>Private lessons cater to a swimmer's specific objectives with a customized training plan created by one of our qualified private lesson instructors.</p>
                    </div>
                </a>
            </div>

        </div>

    </div>
</main>